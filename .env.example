# Google AI Studio API Key
# Get your key at: https://aistudio.google.com/app/apikey
GOOGLE_API_KEY=your_google_api_key_here

# Optional: Set to 'development' or 'production'
ENVIRONMENT=development

# Optional: Custom rate limiting (default: 15 RPM for free tier)
MAX_REQUESTS_PER_MINUTE=15

# Optional: Scraping timeout in seconds (default: 30)
SCRAPE_TIMEOUT=30

# Optional: Maximum news articles to analyze per session (default: 5)
MAX_NEWS_SOURCES=5